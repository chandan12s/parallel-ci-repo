# proj1

A very small example Java project built with Maven.

Build:

```bash
mvn package
```

Run (after build):

```bash
java -cp target/proj1-1.0-SNAPSHOT.jar com.example.App
```

This project contains a single class `com.example.App` that prints "Hello, World!".
